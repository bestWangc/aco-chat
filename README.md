# Aco Chat

Flutter client for the Aco project.

The project targets Android and iOS only.

## Run

```bash
flutter pub get
flutter run
```

The API base URL defaults to `http://localhost:8080/api/v1`. Platform-specific
configuration can be added in `lib/core/config/app_config.dart` as API features
are introduced.
